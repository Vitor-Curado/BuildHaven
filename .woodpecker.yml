pipeline:
  build:
    image: rust:latest
    commands:
      - ./scripts/check.sh

  deploy:
    image: appleboy/drone-ssh
    depends_on:
      - build
    settings:
      host: 167.99.180.229
      username: root
      key:
        from_secret: ssh_key
      script:
        - cd /srv/Personal-website
        - ./scripts/deploy.sh